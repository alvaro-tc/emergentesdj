# Copia este archivo a .env y ajusta los valores

# URL del backend Django (termina en /api/)
# En CRA se usaba REACT_APP_BACKEND_SERVER — sigue funcionando via vite.config.js define
REACT_APP_BACKEND_SERVER=http://localhost:5000/api/
